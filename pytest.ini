[pytest]
asyncio_mode = strict
pythonpath = src
markers =
    integration: requires Docker (Qdrant + Neo4j) and crawl4ai/playwright
    real_web: requires live internet, Qdrant, Neo4j, and OPENAI_API_KEY
